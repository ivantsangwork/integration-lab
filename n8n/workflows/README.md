# Exported n8n workflows

Each workflow here is exported from the self-hosted n8n instance and served as a
downloadable file on the site. A client can import the JSON into their own n8n
and run it — which is a stronger proof than a screenshot of a canvas.

Strip credentials before exporting. n8n substitutes credential *references*, not
values, but check the JSON before committing it.
